name: GitHub Rulesets

on:
    workflow_dispatch:

      jobs:
          apply_rulesets:
                runs-on: ubuntu-latest
                    steps:
                            - name: Apply repo rules
                                    run: echo "Configure GitHub rulesets manually via settings or REST API"

